<div class="m-4">   
           {# 
           {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}     si on veut enlever la validation côté client  (dans ce cas tous les champs de l'entity
                                                                                                                             doivent être nullables car la vérification se fait
                                                                                                                             après hydratation de l'objet)
           #}
            {{ form_start(form) }}
    <div class="row">
        <div class="col-md-4">{{ form_row(form.title) }}</div>
        <div class="col-md-4">{{ form_row(form.surface) }}</div>
        <div class="col-md-4">{{ form_row(form.price) }}</div>
    </div>
    <div class="row">
        <div class="col-md-2">{{ form_row(form.rooms) }}</div>
        <div class="col-md-2">{{ form_row(form.bedrooms) }}</div>
        <div class="col-md-2">{{ form_row(form.floor) }}</div>
        <div class="col-md-2">{{ form_row(form.heat) }}</div>
    </div>
    <div class="row">
        {{ form_row(form.options) }}
    </div>
    <div class="row">
        <div class="col-md-5">{{ form_row(form.address) }}</div>
        <div class="col-md-2">{{ form_row(form.city) }}</div>
        <div class="col-md-1">{{ form_row(form.postal_code) }}</div>
        <div class="col-md-2">{{ form_row(form.sold) }}</div>
    </div>

    <div class="row">
                {{ form_row(form.description) }}
    </div>
    <div class="row">
                {{ form_row(form.imageFile) }}
    </div>
    
            <button class="btn btn-primary" type="submit">{{ button|default('valider')}}</button>
            {{ form_end(form) }}
    </div>